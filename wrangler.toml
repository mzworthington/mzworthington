# Cloudflare Pages: static deploy (build happens in CI).
# Local preview: bundle exec jekyll build && npx wrangler pages dev _site --config wrangler.toml
name = "mzworthington"
compatibility_date = "2026-08-08"
pages_build_output_dir = "_site"
